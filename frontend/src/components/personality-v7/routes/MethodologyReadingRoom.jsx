import React, { useState } from 'react';
import MediaPlane from '../motion/MediaPlane';
import { MEDIA_ASSETS_V7 } from '../../../content/personality-v7/mediaManifest';
import { PUBLIC_CONTENT } from '../../../content/personality-v7/publicContent';
const frameworks = [['Big Five', 'Five personality dimensions are read as separate spectra.', 'The framework offers dimensions for reflection; it does not turn a person into a type.'], ['RIASEC', 'Six interest environments describe different kinds of activity and problems.', 'Interest environments are a vocabulary for exploration, not a career prescription.'], ['Work values', 'Conditions such as achievement, independence and support can be compared separately.', 'Values identify questions worth asking about work conditions, not a universal hierarchy.'], ['Behavioural signals', 'Response choices and stated context can remain traceable in the record.', 'A trace explains the source of a reading; it does not claim to capture a whole person.']];
export const MethodologyReadingRoom = () => { const [open, setOpen] = useState(null); const data = PUBLIC_CONTENT.methodology; return <section className="pa-method-room" aria-labelledby="method-room-title"><header><MediaPlane asset={MEDIA_ASSETS_V7.a07} priority alt="Working evidence collage" /><div><h1 id="method-room-title">{data.title}</h1><p>{data.lead}</p></div></header><div className="pa-method-room__regions">{frameworks.map(([title, summary, detail], index) => <article key={title}><h2>{title}</h2><p>{summary}</p><button aria-expanded={open === index} onClick={() => setOpen(open === index ? null : index)}>{open === index ? 'Hide detail' : 'Read detail'}</button>{open === index && <p className="pa-method-room__detail">{detail}</p>}</article>)}</div><aside><h2>Limits matter.</h2>{data.limitations.map((item) => <p key={item.heading}><strong>{item.heading}</strong>{item.body}</p>)}</aside></section>; };
export default MethodologyReadingRoom;
