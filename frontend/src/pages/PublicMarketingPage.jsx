import { useState } from 'react';
import { Link } from 'react-router-dom';
import { marketingDemo, methodology, publicMedia } from '../content/personalityMarketingDemo';
import { PublicLayout, ResponsiveImage } from '../components/public/PublicChrome';
import './PublicSite.css';

const copy = {
  'how-it-works': ['HOW IT WORKS', 'FROM WORK<br />TO A READING<br />YOU CAN INSPECT.', 'Professional context, assessment responses and behaviour each make a distinct contribution.'],
  'career-intelligence': ['CAREER INTELLIGENCE', 'CAREER FIT<br />SHOULD SHOW<br />ITS REASONING.', 'A curated comparison gives direction a visible basis: alignment, difference and development action.'],
  progress: ['PROGRESS', 'A PROFILE<br />IS A CURRENT<br />STATE.', 'It becomes more useful when you can see what has changed and what you did next.'],
  methodology: ['METHODOLOGY', 'WHAT THE SYSTEM<br />MEASURES—<br />AND WHAT IT DOESN’T.', 'A restrained framework for professional reflection, not diagnosis or prediction.'],
  trust: ['TRUST & TRANSPARENCY', 'USEFUL ONLY<br />WHEN ITS BOUNDARIES<br />ARE CLEAR.', 'The product explains its evidence, confidence and limits in plain language.'],
  privacy: ['PRIVACY & DATA CONTROL', 'YOUR DATA<br />SHOULD STAY YOURS.', 'Clear actions for the information held in your account.'],
};

function Hero({ type, image }) { const [eyebrow, title, intro] = copy[type]; return <section className={`pa-page-hero pa-page-hero--${type}`}><div><p className="pa-kicker">{eyebrow}</p><h1 dangerouslySetInnerHTML={{ __html: title }} /><p>{intro}</p></div>{image && <ResponsiveImage media={image} folder={type === 'career-intelligence' ? 'careers' : 'work'} alt={type === 'career-intelligence' ? image.alt : ''} priority />}</section>; }

function HowItWorks() { const steps = [['Professional context', 'Add your field, skills, experience and a professional goal. A CV is optional.'], ['Adaptive assessment', 'Respond to questions that build evidence progressively. You can return to a persistent assessment.'], ['Behavioural evidence', 'Scenario and response patterns add a different kind of professional evidence.'], ['Professional portrait', 'Read tendencies, interests, work values, career signals and confidence as distinct lenses.'], ['Career relationship', 'Compare curated careers, inspect differences and make a development plan.']]; return <section className="pa-journey"><div className="pa-journey__image"><ResponsiveImage media={publicMedia.work[3]} alt="Research material and a workspace" /></div><ol>{steps.map(([title, text], index) => <li key={title}><span>0{index + 1}</span><h2>{title}</h2><p>{text}</p></li>)}</ol><aside><span>Adaptive question · demonstration</span><strong>When a project changes direction, what do you do first?</strong><button type="button">Clarify the changed constraint, then test the revised path.</button><button type="button">Gather input before choosing the next step.</button></aside></section>; }

function Career() { const [career, setCareer] = useState(marketingDemo.careers[0]); return <><section className="pa-career-scene"><ResponsiveImage media={publicMedia.careers.software} folder="careers" alt={publicMedia.careers.software.alt} /><div><p className="pa-kicker">CURATED CAREER COMPARISON</p><h2>{career.name.toUpperCase()}</h2><p>{career.fit}</p><dl><div><dt>Why it relates</dt><dd>{career.why}</dd></div><div><dt>Development difference</dt><dd>{career.gap}</dd></div></dl></div></section><section className="pa-career-switch"><p>Explore another relationship</p><div role="group" aria-label="Career examples">{marketingDemo.careers.map((item) => <button type="button" onClick={() => setCareer(item)} className={career.name === item.name ? 'is-active' : ''} key={item.name}>{item.name}</button>)}</div><p>Career alignment is a deterministic comparison against curated profiles. It is not a prediction of employability, a hiring recommendation or a guarantee.</p></section></>; }

function Progress() { return <><section className="pa-progress-story"><div><p className="pa-kicker">ASSESSMENT HISTORY</p><h2>THE RECORD<br />CARRIES FORWARD.</h2><p>Profile information, roadmap work and later readings belong in the same professional story.</p></div><ResponsiveImage media={publicMedia.work[4]} alt="Precision testing work in progress" /><ol>{['First assessment', 'Current development work', 'Next reading'].map((step, index) => <li key={step}><span>0{index + 1}</span>{step}</li>)}</ol></section><section className="pa-progress-detail"><div><p className="pa-kicker">CURRENT DEVELOPMENT</p><h2>Signals and skills are not the same thing.</h2></div>{marketingDemo.roadmap.map(([label, detail]) => <article key={label}><h3>{label}</h3><p>{detail}</p></article>)}</section></>; }

function Methodology() { const families = [['BIG FIVE / OCEAN', '5 dimensions', methodology.bigFive], ['RIASEC', '6 dimensions', methodology.riasec], ['WORK VALUES', '8 dimensions', methodology.values], ['CAREER SIGNALS', '4 dimensions', methodology.signals]]; return <><section className="pa-method-intro"><ResponsiveImage media={publicMedia.work[0]} alt="Professional planning and analysis" /><p>Assessment families remain distinct, so you can understand the source of a reading rather than receive one opaque label.</p></section><section className="pa-method-chapters">{families.map(([title, count, items], index) => <article key={title}><span>0{index + 1}</span><div><p className="pa-kicker">{count}</p><h2>{title}</h2><ul>{items.map((item) => <li key={item}>{item}</li>)}</ul></div></article>)}</section><section className="pa-method-boundary"><h2>CONFIDENCE DESCRIBES<br />EVIDENCE QUALITY.</h2><p>Career comparison is deterministic against curated careers. Optional AI can support narrative and coaching only where available; it does not determine core scoring.</p></section></>; }

function Trust() { const points = [['Not a clinical diagnosis', 'Personality Assessor supports professional reflection. It does not diagnose personality or mental health.'], ['Not a guarantee of employment success', 'A career relationship is not a promise of job placement, performance or satisfaction.'], ['Career fit is an alignment comparison', 'It compares the current profile with curated career profiles across visible factors.'], ['Confidence describes evidence quality', 'Confidence provides context about available evidence; it is not a statement of certainty.'], ['AI is optional to the core experience', 'Where used, AI supports narrative or coaching. It does not replace deterministic scoring.']]; return <section className="pa-commitments">{points.map(([title, text], index) => <article key={title}><span>0{index + 1}</span><h2>{title}</h2><p>{text}</p></article>)}</section>; }

function Privacy() { const actions = [['Export', 'Request a copy of the data available through your account.'], ['Delete profile data', 'Remove profile information through the supported account controls.'], ['Delete an assessment', 'Delete individual assessment records where the product provides that action.'], ['Delete account', 'Confirm account deletion through the explicit product flow.']]; return <section className="pa-privacy-actions"><div><p className="pa-kicker">ACCOUNT CONTROLS</p><h2>CLEAR ACTIONS.<br />NO GUESSWORK.</h2><p>Privacy controls sit inside your account, where actions can be confirmed intentionally.</p><Link className="pa-button pa-button--dark" to="/login">Sign in to manage data</Link></div><ol>{actions.map(([title, text], index) => <li key={title}><span>0{index + 1}</span><h3>{title}</h3><p>{text}</p></li>)}</ol></section>; }

const bodies = { 'how-it-works': HowItWorks, 'career-intelligence': Career, progress: Progress, methodology: Methodology, trust: Trust, privacy: Privacy };
export default function PublicMarketingPage({ type }) { const Body = bodies[type]; const image = type === 'career-intelligence' ? publicMedia.careers.ux : type === 'progress' ? publicMedia.work[2] : null; return <PublicLayout page={type}><main id="main-content" className={`pa-marketing pa-marketing--${type}`}><Hero type={type} image={image} /><Body /></main></PublicLayout>; }
